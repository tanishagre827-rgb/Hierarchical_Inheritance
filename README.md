# Hierarchical_Inheritance s a type of inheritance in Java where multiple child classes inherit from a single parent class. This concept helps in code reusability and better organization of related classes.
In this project, a common base class is used to share properties and methods among different derived classes, demonstrating how hierarchical inheritance works in Java.
